name: Bootstrap StockPromt from StockForge AI

# Historical bootstrap helper. Deliberately manual-only so the new StockPromt
# project cannot overwrite its own work on every push.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  copy-source:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical bootstrap disabled for the standalone StockPromt project"
